<Window x:Class="MyWixV4WpfBA.BA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="My WiX v4 WPF BA" Height="260" Width="520">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="18" FontWeight="SemiBold"
                   Text="My WiX v4 WPF BootstrapperApplication" />

        <TextBlock Grid.Row="1" Margin="0,8,0,0" TextWrapping="Wrap"
                   Text="This is a minimal placeholder UI. Wire up commands/state in MyBootstrapperApplication." />

        <TextBox Grid.Row="2" Margin="0,12,0,12" IsReadOnly="True" TextWrapping="Wrap"
                 Text="TODO: display detect/plan/apply progress here." />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="100" Margin="0,0,8,0" Content="Install" />
            <Button Width="100" Content="Exit" Click="Exit_Click" />
        </StackPanel>
    </Grid>
</Window>
